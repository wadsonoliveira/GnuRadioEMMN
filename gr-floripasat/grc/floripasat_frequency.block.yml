id: floripasat_frequency
label: Frequency Static
category: '[floripasat]'

templates:
  imports: import floripasat
  make: floripasat.frequency(${freq})

parameters:
- id: freq
  label: Frequency
  dtype: float

outputs:
- label: freq
  domain: stream
  dtype: float

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
